name: rill
root: .

windows:
  - services:
      root: ./
      panes:
        - api:
          - go run cmd/rill/main.go web
        - yarn:
          - cd frontend
          - yarn start
        - worker:
          - go run cmd/rill/main.go worker
  - web/editor:
      root: ./frontend
      panes:
        - vim src/index.tsx
  - api/editor:
      root: ./
      panes:
        - vim cmd/rill/main.go
  - database:
      root: ./
      panes:
        - pgcli postgres://rill:rill_test@localhost:8855/rill
